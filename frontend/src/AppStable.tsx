import { useEffect, useState } from 'react';
import { Archive, Check, Circle, ClipboardList, Command, Download, FileText, FolderKanban, Moon, Plus, Settings, Sparkles, Sun, Target, Trash2, Upload, Users } from 'lucide-react';

type View='Dashboard'|'Projects'|'Notes'|'Tasks'|'Decisions'|'Weekly'|'People'|'Templates'|'Activity'|'Settings';
type Role='CEO'|'CTO'|'CTPO'|'CGO';
type Priority='Low'|'Medium'|'High';
type TaskStatus='Todo'|'In Progress'|'Done';
type ProjectStatus='Idea'|'Active'|'Paused'|'Completed';
type Project={id:string;name:string;description:string;status:ProjectStatus;priority:Priority;owner?:string};
type Note={id:string;projectId:string;title:string;content:string};
type Task={id:string;projectId:string;title:string;description:string;status:TaskStatus;priority:Priority;assignedTo?:string;assignedExternalId?:string;dueDate?:string};
type Decision={id:string;projectId:string;title:string;decision:string;consequences:string};
type Weekly={id:string;projectId:string;title:string;done:boolean};
type Member={id:string;name:string;email:string;role:Role};
type Stakeholder={id:string;projectId:string;name:string;company:string;email:string;phone:string;stakeholderType:string;role:string;status:string;notes:string};
type Activity={id:string;entity_type:string;action:string;created_at:string;name?:string;email?:string};
type Store={projects:Project[];notes:Note[];tasks:Task[];decisions:Decision[];weekly:Weekly[];stakeholders:Stakeholder[]};
type Template={name:string;description:string;tasks:string[];decisions:string[];weekly:string[];notes:string[];stakeholders?:string[]};
const TOKEN_KEY='launchx-project-os:api-token';const API_KEY='launchx-project-os:api-base';const LOCAL_KEY='launchx-project-os:stable-local';const defaultApi=import.meta.env.VITE_API_BASE_URL||'https://api.launchx.in';const uid=()=>crypto.randomUUID();
const seed:Store={projects:[{id:'p1',name:'LaunchX Project OS',description:'Shared CXO workspace for execution.',status:'Active',priority:'High',owner:'CEO'}],notes:[{id:'n1',projectId:'p1',title:'Product thesis',content:'## Project OS\nEditable v1 workspace.'}],tasks:[{id:'t1',projectId:'p1',title:'Run v1 smoke test',description:'Validate edit, due date, assignment and dashboard.',status:'Todo',priority:'High'}],decisions:[{id:'d1',projectId:'p1',title:'ADR-001: CEO-owned workspace',decision:'CEO owns destructive operations.',consequences:'Team contributes safely.'}],weekly:[{id:'w1',projectId:'p1',title:'Freeze v1 after smoke test',done:false}],stakeholders:[]};
const projectTemplates:Template[]=[{name:'Triphulu',description:'Travel SaaS and map-led package discovery platform.',tasks:['Ship map milestone','Define package model','Create supplier onboarding flow'],decisions:['ADR: Map provider','ADR: Package pricing logic'],weekly:['Complete map by 25th'],notes:['Triphulu launch thesis'],stakeholders:['Hotel Partner','Transport Vendor','Travel Guide']},{name:'LaunchX',description:'MSME/founder consulting execution workspace.',tasks:['Package advisory offers','Build lead pipeline','Create delivery template'],decisions:['ADR: Service packages'],weekly:['Publish offer page'],notes:['LaunchX positioning'],stakeholders:['Client','CA Firm','Marketing Partner']},{name:'KitchenOS',description:'Restaurant SaaS for POS and kitchen workflows.',tasks:['Define POS workflows','Build order board','Setup restaurant demo'],decisions:['ADR: POS boundary'],weekly:['Ship demo flow'],notes:['KitchenOS thesis'],stakeholders:['Restaurant Owner','Kitchen Staff']},{name:'Pin Code Café',description:'Dhenkanal café brand and operations.',tasks:['Finalize menu','Setup operations','Create launch campaign'],decisions:['ADR: Coffee sourcing'],weekly:['Complete café readiness'],notes:['Café brand story'],stakeholders:['Coffee Supplier','Designer']},{name:'Odia Nanaa',description:'Odia food brand and restaurant growth project.',tasks:['Finalize seating','Lock menu economics','Setup staff SOP'],decisions:['ADR: Menu architecture'],weekly:['Complete outlet setup'],notes:['Odia Nanaa story'],stakeholders:['Food Supplier','Interior Vendor']}];
const epicTemplates:Template[]=[{name:'Travel Package Launch',description:'Plan, price, publish and operate a travel package.',tasks:['Define itinerary','Confirm supplier rates','Create map and route plan','Publish landing page'],decisions:['ADR: Pricing model','ADR: Supplier selection'],weekly:['Lock supplier confirmations'],notes:['Package positioning'],stakeholders:['Hotel Partner','Transport Vendor']},{name:'SaaS Product',description:'Build and ship a SaaS MVP.',tasks:['Define ICP','Create PRD','Build MVP backlog','Ship beta'],decisions:['ADR: Architecture','ADR: Pricing model'],weekly:['Ship MVP slice'],notes:['Product thesis'],stakeholders:['Beta Customer']},{name:'Restaurant Launch',description:'Open and stabilize a restaurant/café.',tasks:['Finalize menu','Confirm vendors','Setup POS','Hire staff'],decisions:['ADR: Menu pricing'],weekly:['Complete outlet readiness'],notes:['Brand story'],stakeholders:['Interior Vendor']},{name:'Marketing Campaign',description:'Campaign execution with creative and measurement.',tasks:['Define campaign goal','Create brief','Finalize calendar','Launch posts'],decisions:['ADR: Channel mix'],weekly:['Publish campaign assets'],notes:['Hook bank'],stakeholders:['Agency']},{name:'Fundraising',description:'Investor readiness and outreach pipeline.',tasks:['Prepare pitch deck','Build model','Create investor list','Send outreach'],decisions:['ADR: Round structure'],weekly:['Close deck v1'],notes:['Fundraising narrative'],stakeholders:['Investor']},{name:'Hiring',description:'Role definition to offer rollout.',tasks:['Define scorecard','Publish post','Screen candidates','Run interviews'],decisions:['ADR: Compensation band'],weekly:['Shortlist candidates'],notes:['Interview rubric'],stakeholders:['Recruiter']},{name:'Customer Implementation',description:'Onboarding and delivery.',tasks:['Kickoff call','Collect requirements','Configure workflow','Run UAT'],decisions:['ADR: Scope boundary'],weekly:['Complete UAT'],notes:['Implementation notes'],stakeholders:['Client SPOC']},{name:'Compliance / Audit',description:'Evidence and audit readiness.',tasks:['Define controls','Collect evidence','Review gaps'],decisions:['ADR: Control owner model'],weekly:['Close high-risk gaps'],notes:['Audit scope'],stakeholders:['Auditor']}];
function loadLocal():Store{try{return JSON.parse(localStorage.getItem(LOCAL_KEY)||'') as Store}catch{return seed}}function badge(v:string){return 'badge '+v.toLowerCase().replaceAll(' ','-')}function md(v:string){return v.replaceAll('&','&amp;').replaceAll('<','&lt;').replace(/^## (.*)$/gm,'<h2>$1</h2>').replace(/^# (.*)$/gm,'<h1>$1</h1>').replace(/\n/g,'<br/>')}function mapWs(w:any):Store{return{projects:(w.projects||[]).map((p:any)=>({id:p.id,name:p.name,description:p.description||'',status:p.status,priority:p.priority,owner:p.owner_user_id?'Team':'CEO'})),notes:(w.notes||[]).map((n:any)=>({id:n.id,projectId:n.project_id||'',title:n.title,content:n.content||''})),tasks:(w.tasks||[]).map((t:any)=>({id:t.id,projectId:t.project_id||'',title:t.title,description:t.description||'',status:t.status,priority:t.priority,assignedTo:t.assigned_to||undefined,assignedExternalId:t.assigned_external_id||undefined,dueDate:t.due_date||undefined})),decisions:(w.decisions||[]).map((d:any)=>({id:d.id,projectId:d.project_id||'',title:d.title,decision:d.decision||'',consequences:d.consequences||''})),weekly:(w.weekly||[]).map((x:any)=>({id:x.id,projectId:x.project_id||'',title:x.title,done:Boolean(x.done)})),stakeholders:(w.stakeholders||[]).map((s:any)=>({id:s.id,projectId:s.project_id||'',name:s.name,company:s.company||'',email:s.email||'',phone:s.phone||'',stakeholderType:s.stakeholder_type||'External',role:s.role||'',status:s.status||'Active',notes:s.notes||''}))}}
export default function AppStable(){const[data,setData]=useState<Store>(loadLocal);const[view,setView]=useState<View>('Dashboard');const[projectId,setProjectId]=useState(data.projects[0]?.id||'');const[token,setToken]=useState(localStorage.getItem(TOKEN_KEY)||'');const[apiBase,setApiBase]=useState(localStorage.getItem(API_KEY)||defaultApi);const[email,setEmail]=useState('ajit@theajit.in');const[password,setPassword]=useState('');const[dark,setDark]=useState(localStorage.getItem('theme')!=='light');const[members,setMembers]=useState<Member[]>([]);const[activity,setActivity]=useState<Activity[]>([]);const[userRole,setUserRole]=useState<Role|null>(null);const[userId,setUserId]=useState('');const[syncStatus,setSyncStatus]=useState(token?'Connected':'Local only');const[newMember,setNewMember]=useState({name:'',email:'',password:'',role:'CTO' as Exclude<Role,'CEO'>});const[newStakeholder,setNewStakeholder]=useState({name:'',company:'',email:'',phone:'',stakeholderType:'Vendor',role:'',status:'Active',notes:''});const currentProject=data.projects.find(p=>p.id===projectId);const projectTasks=data.tasks.filter(t=>t.projectId===projectId);const projectNotes=data.notes.filter(n=>n.projectId===projectId);const projectDecisions=data.decisions.filter(d=>d.projectId===projectId);const projectWeekly=data.weekly.filter(w=>w.projectId===projectId);const projectStakeholders=data.stakeholders.filter(s=>s.projectId===projectId);const isCEO=userRole==='CEO';const overdue=projectTasks.filter(t=>t.dueDate&&new Date(t.dueDate)<new Date()&&t.status!=='Done');const dueWeek=projectTasks.filter(t=>t.dueDate&&t.status!=='Done');const blocked=projectTasks.filter(t=>t.assignedExternalId&&t.status!=='Done');const myWork=projectTasks.filter(t=>t.status!=='Done'&&(!userId||t.assignedTo===userId||(!t.assignedTo&&!t.assignedExternalId))).slice(0,6);useEffect(()=>localStorage.setItem(LOCAL_KEY,JSON.stringify(data)),[data]);useEffect(()=>{document.documentElement.classList.toggle('dark',dark);localStorage.setItem('theme',dark?'dark':'light')},[dark]);useEffect(()=>{if(!token)return;void refreshWorkspace();const id=setInterval(()=>void refreshWorkspace(false),30000);return()=>clearInterval(id)},[token]);
async function api(path:string,body?:unknown,method?:string,auth=true){const res=await fetch(`${apiBase.replace(/\/$/,'')}${path}`,{method:method||(body?'POST':'GET'),headers:{'Content-Type':'application/json',...(auth&&token?{Authorization:`Bearer ${token}`}:{})},body:body?JSON.stringify(body):undefined});const json=await res.json();if(!res.ok)throw new Error(json.message||json.error||'API failed');return json}async function refreshWorkspace(show=true,nextToken=token){if(!nextToken)return;try{const base=apiBase.replace(/\/$/,'');const[meRes,wsRes,actRes]=await Promise.all([fetch(`${base}/me`,{headers:{Authorization:`Bearer ${nextToken}`}}),fetch(`${base}/workspace`,{headers:{Authorization:`Bearer ${nextToken}`}}),fetch(`${base}/activity`,{headers:{Authorization:`Bearer ${nextToken}`}})]);const me=await meRes.json();const ws=await wsRes.json();const act=await actRes.json();if(!wsRes.ok)throw new Error(ws.message||'Workspace load failed');const next=mapWs(ws);setData(next);setMembers(ws.members||[]);setActivity(Array.isArray(act)?act:[]);setUserRole(me.role||null);setUserId(me.id||'');if(!next.projects.find(p=>p.id===projectId))setProjectId(next.projects[0]?.id||'');if(show)setSyncStatus(`Auto-synced ${new Date().toLocaleTimeString()}`)}catch(e){setSyncStatus(e instanceof Error?e.message:'Sync failed')}}async function login(){try{const json=await api('/auth/login',{email,password},undefined,false);setToken(json.token);localStorage.setItem(TOKEN_KEY,json.token);localStorage.setItem(API_KEY,apiBase);await refreshWorkspace(true,json.token)}catch(e){setSyncStatus(e instanceof Error?e.message:'Login failed')}}async function bootstrap(){try{const json=await api('/auth/bootstrap',{organizationName:'LaunchX',name:'Ajit Satpathy',email,password,role:'CEO'},undefined,false);setToken(json.token);localStorage.setItem(TOKEN_KEY,json.token);localStorage.setItem(API_KEY,apiBase);await refreshWorkspace(true,json.token)}catch(e){setSyncStatus(e instanceof Error?e.message:'Bootstrap failed')}}function logout(){setToken('');localStorage.removeItem(TOKEN_KEY);setMembers([]);setUserRole(null);setSyncStatus('Signed out')}async function save(path:string,body:unknown,local:()=>void,method='PATCH'){try{if(token){await api(path,body,method);await refreshWorkspace();return}local()}catch(e){setSyncStatus(e instanceof Error?e.message:'Save failed');local()}}async function create(path:string,body:unknown,local:()=>void){try{if(token){await api(path,body);await refreshWorkspace();return}local()}catch(e){setSyncStatus(e instanceof Error?e.message:'Create failed');local()}}async function remove(path:string,local:()=>void){try{if(token){await api(path,undefined,'DELETE');await refreshWorkspace();return}local()}catch(e){setSyncStatus(e instanceof Error?e.message:'Delete failed')}}function updateLocal<K extends keyof Store>(key:K,id:string,changes:object){setData(d=>({...d,[key]:(d[key] as any[]).map(x=>x.id===id?{...x,...changes}:x)}))}
async function seedTemplate(targetProjectId:string,template?:Template){if(!template||!token)return;await Promise.all([...template.tasks.map(title=>api('/tasks',{projectId:targetProjectId,title,description:'Template task',status:'Todo',priority:'Medium'})),...template.decisions.map(title=>api('/decisions',{projectId:targetProjectId,title,decision:'To be decided',consequences:'Capture impact.'})),...template.weekly.map(title=>api('/weekly-priorities',{projectId:targetProjectId,title})),...template.notes.map(title=>api('/notes',{projectId:targetProjectId,title,content:`## ${title}`})),...(template.stakeholders||[]).map(name=>api('/stakeholders',{projectId:targetProjectId,name,stakeholderType:'External',role:'Stakeholder'}))])}async function addProject(template?:Template){const item:Project={id:uid(),name:template?.name||'New project',description:template?.description||'',status:'Idea',priority:'Medium',owner:'CEO'};if(token){const p=await api('/projects',{name:item.name,description:item.description,status:item.status,priority:item.priority});await seedTemplate(p.id,template);await refreshWorkspace();setProjectId(p.id)}else{setData(d=>({...d,projects:[item,...d.projects]}));setProjectId(item.id)}setView('Projects')}async function applyEpic(t:Template){await seedTemplate(projectId,t);await refreshWorkspace();setView('Dashboard')}async function addNote(){const n:Note={id:uid(),projectId,title:'Untitled note',content:'## New note'};await create('/notes',{projectId,title:n.title,content:n.content},()=>setData(d=>({...d,notes:[n,...d.notes]})));setView('Notes')}async function addTask(status:TaskStatus){const t:Task={id:uid(),projectId,title:'New task',description:'',status,priority:'Medium'};await create('/tasks',{projectId,title:t.title,description:t.description,status,priority:t.priority},()=>setData(d=>({...d,tasks:[t,...d.tasks]})));setView('Tasks')}async function addDecision(){const x:Decision={id:uid(),projectId,title:'ADR: New decision',decision:'',consequences:''};await create('/decisions',{projectId,title:x.title,decision:x.decision,consequences:x.consequences},()=>setData(d=>({...d,decisions:[x,...d.decisions]})));setView('Decisions')}async function addWeekly(){const w:Weekly={id:uid(),projectId,title:'New weekly priority',done:false};await create('/weekly-priorities',{projectId,title:w.title,done:false},()=>setData(d=>({...d,weekly:[w,...d.weekly]})));setView('Weekly')}async function addStakeholder(){const s:Stakeholder={id:uid(),projectId,...newStakeholder};await create('/stakeholders',{projectId,...newStakeholder},()=>setData(d=>({...d,stakeholders:[s,...d.stakeholders]})));setNewStakeholder({name:'',company:'',email:'',phone:'',stakeholderType:'Vendor',role:'',status:'Active',notes:''})}async function addMember(){if(!isCEO)return setSyncStatus('Only CEO can create team members');await api('/team/members',newMember);setNewMember({name:'',email:'',password:'',role:'CTO'});await refreshWorkspace()}async function assignTask(task:Task,target:string){const[type,value]=target.split(':');await save(`/tasks/${task.id}/assign`,{assignedTo:type==='user'?value:null,assignedExternalId:type==='external'?value:null},()=>updateLocal('tasks',task.id,{assignedTo:type==='user'?value:undefined,assignedExternalId:type==='external'?value:undefined}),'POST')}function assignee(t:Task){if(t.assignedExternalId)return projectStakeholders.find(s=>s.id===t.assignedExternalId)?.name||'External';if(t.assignedTo)return members.find(m=>m.id===t.assignedTo)?.name||'Internal';return'Unassigned'}function exportJson(){const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([JSON.stringify(data,null,2)],{type:'application/json'}));a.download='launchx-project-os.json';a.click()}function importJson(file?:File){if(!file)return;const r=new FileReader();r.onload=()=>setData(JSON.parse(String(r.result)));r.readAsText(file)}
return <div className="app-shell"><aside className="sidebar"><div className="brand"><div className="brand-mark">LX</div><div><b>LaunchX</b><span>Project OS</span></div></div><Nav view={view} setView={setView}/><button className="command-hint"><Command size={15}/> Ctrl K</button></aside><main><header className="topbar compact-topbar"><div><p className="eyebrow">{token?`${userRole||'Team'} · auto-sync`:'Local-first'}</p><h1>{view}</h1><p className="subhead">{currentProject?.name||'No project'} · {syncStatus}</p></div><button className="theme-float" onClick={()=>setDark(!dark)}>{dark?<Sun/>:<Moon/>}</button><div className="top-actions"><select value={projectId} onChange={e=>setProjectId(e.target.value)}>{data.projects.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select><button className="primary compact-add" onClick={()=>view==='Tasks'?void addTask('Todo'):view==='Notes'?void addNote():view==='Decisions'?void addDecision():view==='Weekly'?void addWeekly():view==='People'?void addStakeholder():void addProject()}><Plus/> Add</button></div></header>{view==='Dashboard'&&<section className="dashboard dashboard-compact"><div className="hero card hero-compact"><span className="pill"><Sparkles size={14}/> v1 cockpit</span><h2>{currentProject?.name}</h2><p>{currentProject?.description}</p></div><div className="shortcut-grid"><Shortcut label="Tasks" value={projectTasks.filter(t=>t.status!=='Done').length} detail="open" icon={<ClipboardList/>} onClick={()=>setView('Tasks')}/><Shortcut label="People" value={members.length+projectStakeholders.length} detail="internal + external" icon={<Users/>} onClick={()=>setView('People')}/><Shortcut label="ADRs" value={projectDecisions.length} detail="decisions" icon={<Archive/>} onClick={()=>setView('Decisions')}/><Shortcut label="Weekly" value={projectWeekly.length} detail="priorities" icon={<Target/>} onClick={()=>setView('Weekly')}/></div><Panel title="My work">{myWork.map(t=><button className="list-card" key={t.id} onClick={()=>setView('Tasks')}><b>{t.title}</b><span>{assignee(t)} · {t.priority} {t.dueDate?`· due ${String(t.dueDate).slice(0,10)}`:''}</span></button>)}</Panel><Panel title="Due / blocked"><button className="list-card" onClick={()=>setView('Tasks')}><b>{overdue.length} overdue · {dueWeek.length} due</b><span>{blocked.length} waiting on external stakeholders</span></button></Panel><Panel title="Recent decisions">{projectDecisions.slice(0,4).map(d=><button className="list-card" key={d.id} onClick={()=>setView('Decisions')}><b>{d.title}</b><span>{d.decision}</span></button>)}</Panel><Panel title="Activity timeline">{activity.slice(0,4).map(a=><button className="list-card" key={a.id}><b>{a.name||a.email||'Team'} {a.action} {a.entity_type}</b><span>{new Date(a.created_at).toLocaleString()}</span></button>)}</Panel></section>}{view==='Projects'&&<section className="cards">{data.projects.map(p=><article className="entity-card" key={p.id}><input value={p.name} onChange={e=>updateLocal('projects',p.id,{name:e.target.value})}/><textarea value={p.description} onChange={e=>updateLocal('projects',p.id,{description:e.target.value})}/><div className="settings-grid"><select value={p.status} onChange={e=>updateLocal('projects',p.id,{status:e.target.value})}><option>Idea</option><option>Active</option><option>Paused</option><option>Completed</option></select><select value={p.priority} onChange={e=>updateLocal('projects',p.id,{priority:e.target.value})}><option>Low</option><option>Medium</option><option>High</option></select><button onClick={()=>void save(`/projects/${p.id}`,p,()=>undefined)}>Save</button><button className="mini-danger" onClick={()=>void remove(`/projects/${p.id}`,()=>setData(d=>({...d,projects:d.projects.filter(x=>x.id!==p.id)})))}><Trash2 size={14}/> Delete</button></div></article>)}</section>}{view==='Notes'&&<section><button className="primary" onClick={()=>void addNote()}><Plus/> Note</button><div className="cards">{projectNotes.map(n=><article className="entity-card note-card" key={n.id}><input value={n.title} onChange={e=>updateLocal('notes',n.id,{title:e.target.value})}/><textarea value={n.content} onChange={e=>updateLocal('notes',n.id,{content:e.target.value})}/><div className="markdown compact" dangerouslySetInnerHTML={{__html:md(n.content)}}/><div className="settings-grid"><button onClick={()=>void save(`/notes/${n.id}`,n,()=>undefined)}>Save</button><button className="mini-danger" onClick={()=>void remove(`/notes/${n.id}`,()=>setData(d=>({...d,notes:d.notes.filter(x=>x.id!==n.id)})))}><Trash2 size={14}/> Delete</button></div></article>)}</div></section>}{view==='Tasks'&&<section className="board">{(['Todo','In Progress','Done'] as TaskStatus[]).map(status=><div className="column" key={status}><div className="column-head"><h3>{status}</h3><button onClick={()=>void addTask(status)}><Plus size={15}/></button></div>{projectTasks.filter(t=>t.status===status).map(t=><article className="task-card" key={t.id}><input value={t.title} onChange={e=>updateLocal('tasks',t.id,{title:e.target.value})}/><textarea value={t.description} onChange={e=>updateLocal('tasks',t.id,{description:e.target.value})}/><select value={t.priority} onChange={e=>updateLocal('tasks',t.id,{priority:e.target.value})}><option>Low</option><option>Medium</option><option>High</option></select><select value={t.assignedExternalId?`external:${t.assignedExternalId}`:t.assignedTo?`user:${t.assignedTo}`:''} onChange={e=>void assignTask(t,e.target.value)}><option value="">Unassigned</option><optgroup label="Internal">{members.map(m=><option key={m.id} value={`user:${m.id}`}>{m.name} · {m.role}</option>)}</optgroup><optgroup label="External">{projectStakeholders.map(s=><option key={s.id} value={`external:${s.id}`}>{s.name} · {s.stakeholderType}</option>)}</optgroup></select><label>Due date<input type="date" value={t.dueDate?String(t.dueDate).slice(0,10):''} onChange={e=>updateLocal('tasks',t.id,{dueDate:e.target.value||undefined})}/></label><div className="settings-grid"><select value={t.status} onChange={e=>updateLocal('tasks',t.id,{status:e.target.value})}><option>Todo</option><option>In Progress</option><option>Done</option></select><button onClick={()=>void save(`/tasks/${t.id}`,t,()=>undefined)}>Save</button><button className="mini-danger" onClick={()=>void remove(`/tasks/${t.id}`,()=>setData(d=>({...d,tasks:d.tasks.filter(x=>x.id!==t.id)})))}><Trash2 size={14}/> Delete</button></div><small>Assigned: {assignee(t)}</small></article>)}</div>)}</section>}{view==='Decisions'&&<section className="timeline">{projectDecisions.map(x=><article className="decision-card" key={x.id}><input value={x.title} onChange={e=>updateLocal('decisions',x.id,{title:e.target.value})}/><textarea value={x.decision} onChange={e=>updateLocal('decisions',x.id,{decision:e.target.value})}/><textarea value={x.consequences} onChange={e=>updateLocal('decisions',x.id,{consequences:e.target.value})}/><div className="settings-grid"><button onClick={()=>void save(`/decisions/${x.id}`,x,()=>undefined)}>Save</button><button className="mini-danger" onClick={()=>void remove(`/decisions/${x.id}`,()=>setData(d=>({...d,decisions:d.decisions.filter(dn=>dn.id!==x.id)})))}><Trash2 size={14}/> Delete</button></div></article>)}</section>}{view==='Weekly'&&<section className="weekly">{projectWeekly.map(w=><div className="row" key={w.id}><button className="check" onClick={()=>{const done=!w.done;updateLocal('weekly',w.id,{done});void save(`/weekly-priorities/${w.id}`,{...w,done},()=>undefined)}}>{w.done?<Check/>:<Circle/>}</button><input value={w.title} onChange={e=>updateLocal('weekly',w.id,{title:e.target.value})}/><button onClick={()=>void save(`/weekly-priorities/${w.id}`,w,()=>undefined)}>Save</button><button className="mini-danger" onClick={()=>void remove(`/weekly-priorities/${w.id}`,()=>setData(d=>({...d,weekly:d.weekly.filter(x=>x.id!==w.id)})))}><Trash2 size={14}/></button></div>)}</section>}{view==='People'&&<section className="settings settings-v2"><h2>People</h2><div className="settings-section"><h3>Internal CXOs</h3><div className="team-list">{members.map(m=><div className="member-card" key={m.id}><span className={badge(m.role)}>{m.role}</span><div><b>{m.name}</b><small>{m.email}</small></div></div>)}</div></div><div className="settings-section"><h3>External stakeholders</h3><div className="editor-card"><input placeholder="Name" value={newStakeholder.name} onChange={e=>setNewStakeholder({...newStakeholder,name:e.target.value})}/><input placeholder="Company" value={newStakeholder.company} onChange={e=>setNewStakeholder({...newStakeholder,company:e.target.value})}/><input placeholder="Email" value={newStakeholder.email} onChange={e=>setNewStakeholder({...newStakeholder,email:e.target.value})}/><select value={newStakeholder.stakeholderType} onChange={e=>setNewStakeholder({...newStakeholder,stakeholderType:e.target.value})}><option>Vendor</option><option>Client</option><option>Agency</option><option>Freelancer</option><option>Consultant</option><option>Partner</option></select><button onClick={()=>void addStakeholder()}>Add stakeholder</button></div><div className="team-list">{projectStakeholders.map(s=><div className="member-card" key={s.id}><span className={badge(s.stakeholderType)}>{s.stakeholderType}</span><div><b>{s.name}</b><small>{s.company||s.email||s.role}</small></div><button className="mini-danger" onClick={()=>void remove(`/stakeholders/${s.id}`,()=>setData(d=>({...d,stakeholders:d.stakeholders.filter(x=>x.id!==s.id)})))}><Trash2 size={14}/></button></div>)}</div></div></section>}{view==='Templates'&&<section className="settings settings-v2"><h2>Templates</h2><div className="settings-section"><h3>Project templates</h3><div className="cards">{projectTemplates.map(t=><article className="entity-card" key={t.name}><h3>{t.name}</h3><p>{t.description}</p><button onClick={()=>void addProject(t)}>Create project</button></article>)}</div></div><div className="settings-section"><h3>Epic templates</h3><div className="cards">{epicTemplates.map(t=><article className="entity-card" key={t.name}><h3>{t.name}</h3><p>{t.description}</p><button onClick={()=>void applyEpic(t)}>Apply to selected project</button></article>)}</div></div></section>}{view==='Activity'&&<section className="timeline">{activity.map(a=><article className="decision-card" key={a.id}><h3>{a.entity_type} · {a.action}</h3><p>{a.name||a.email||'Team'} at {new Date(a.created_at).toLocaleString()}</p></article>)}</section>}{view==='Settings'&&<section className="settings settings-v2"><h2>Settings</h2><div className="settings-section"><input value={apiBase} onChange={e=>setApiBase(e.target.value)}/><input value={email} onChange={e=>setEmail(e.target.value)}/><input type="password" value={password} onChange={e=>setPassword(e.target.value)}/><div className="settings-grid"><button onClick={()=>void bootstrap()}>Create CEO workspace</button><button onClick={()=>void login()}>Login</button><button onClick={logout}>Logout</button></div></div><div className="settings-section"><h3>Team</h3><input placeholder="Name" value={newMember.name} onChange={e=>setNewMember({...newMember,name:e.target.value})}/><input placeholder="Email" value={newMember.email} onChange={e=>setNewMember({...newMember,email:e.target.value})}/><input type="password" placeholder="Password" value={newMember.password} onChange={e=>setNewMember({...newMember,password:e.target.value})}/><select value={newMember.role} onChange={e=>setNewMember({...newMember,role:e.target.value as Exclude<Role,'CEO'>})}><option>CTO</option><option>CTPO</option><option>CGO</option></select><button onClick={()=>void addMember()}>Create member</button></div><div className="settings-section"><button onClick={exportJson}><Download/> Export JSON</button><label className="button-like"><Upload/> Import JSON<input type="file" hidden accept="application/json" onChange={e=>importJson(e.target.files?.[0])}/></label></div></section>}</main><nav className="bottom-nav"><Nav view={view} setView={setView} compact/></nav></div>}
function Nav({view,setView}:{view:View;setView:(v:View)=>void;compact?:boolean}){const items:[View,any][]=[['Dashboard',<Sparkles/>],['Projects',<FolderKanban/>],['Notes',<FileText/>],['Tasks',<ClipboardList/>],['Decisions',<Archive/>],['Weekly',<Target/>],['People',<Users/>],['Templates',<Sparkles/>],['Activity',<Command/>],['Settings',<Settings/>]];return <>{items.map(([v,icon])=><button key={v} className={view===v?'active':''} onClick={()=>setView(v)}>{icon}<span>{v}</span></button>)}</>}
function Shortcut({label,value,detail,icon,onClick}:{label:string;value:number;detail:string;icon:any;onClick:()=>void}){return <button className="shortcut-card-link" onClick={onClick}>{icon}<div><b>{value}</b><span>{label}</span><small>{detail}</small></div></button>}
function Panel({title,children}:{title:string;children:any}){return <section className="card panel"><div className="panel-head"><h3>{title}</h3></div>{children}</section>}
