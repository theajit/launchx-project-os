# LaunchX Project OS API

Fastify API scaffold for the server-backed phase of LaunchX Project OS.

## Commands

```bash
npm run api:dev
npm run api:build
npm run api:start
```

The frontend remains Vite-first. The API is restored as a separate Node service so we can add persistence and sync without breaking the local-first MVP.
